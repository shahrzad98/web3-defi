import config from "config/config.json";

export const networks = config.networks;
export const rpc_urls = config.rpc_urls;
export const subgraph = config.subgraph;
export const offchain = config.offchain;
